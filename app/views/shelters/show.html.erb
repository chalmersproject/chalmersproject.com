<div class="flex flex-col gap-2">
  <%= render ShelterComponent.new(shelter: @shelter) %>
  <div class="flex flex-col gap-2">
    <%= link_to "Edit this shelter", edit_shelter_path(@shelter) %>
    <%= link_to "Back to shelters", shelters_path %>
    <%= button_to "Destroy this shelter", @shelter, method: :delete, class: "xt-button danger", form: { data: { turbo_confirm: "Really delete?" } } %>
  </div>
</div>
